# Telegram-бот с поддержкой контекста на основе локальной LLM

## Описание проекта

Telegram-бот, интегрированный с локальной языковой моделью (в моем случае это была Qwen2.5-1.5B-Instruct-GGUF) через LM Studio. Бот поддерживает контекст диалога, сохраняя историю сообщений отдельно для каждого пользователя.

## Демонстрация проекта
https://disk.yandex.ru/i/7IHwCCq8v_kjCw

### Основные функции

- 🤖 **Интеграция с LLM** — бот использует локальную языковую модель через LM Studio API
- 💬 **Поддержка контекста** — бот помнит предыдущие сообщения и отвечает с учётом истории диалога
- 👥 **Многопользовательность** — история диалога хранится отдельно для каждого пользователя
- 🗑️ **Очистка контекста** — возможность начать диалог заново командой `/clear`

## Требования

- Python 3.8+
- LM Studio с запущенным локальным сервером (порт 1234)
- Telegram Bot API токен

## Установка и запуск

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/megavvve/telegram-context-llm-bot
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте LM Studio

1. Скачайте и установите [LM Studio](https://lmstudio.ai/)
2. Загрузите модель (например, `Qwen2.5-1.5B-Instruct-GGUF`) с Hugging Face
3. Запустите локальный сервер в LM Studio (по умолчанию на порту 1234)

### 4. Настройте токен бота

Установите переменную окружения с вашим токеном от @BotFather:

**Windows (PowerShell):**
```powershell
$env:TELEGRAM_BOT_TOKEN="ваш_токен"
```

**Windows (CMD):**
```cmd
set TELEGRAM_BOT_TOKEN=ваш_токен
```

**Linux/macOS:**
```bash
export TELEGRAM_BOT_TOKEN=ваш_токен
```

### 5. Запустите бота

```bash
python main.py
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Вывод приветственного сообщения и списка команд |
| `/model` | Показать название используемой языковой модели |
| `/clear` | Очистить историю диалога и начать заново |

## Структура проекта

```
laba5/
├── main.py           # Основной код бота
├── requirements.txt  # Зависимости проекта
└── README.md         # Документация
```

## Принцип работы контекста

1. При получении сообщения бот находит или создаёт историю диалога для данного `user_id`
2. Новое сообщение пользователя добавляется в историю с ролью `user`
3. Вся история отправляется в LM Studio для генерации ответа
4. Ответ модели добавляется в историю с ролью `assistant`
5. История ограничена последними 20 сообщениями для оптимизации
